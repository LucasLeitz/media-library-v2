server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # If you later build a SPA, this will serve index.html for unknown routes
  location / {
    try_files $uri /index.html;
  }
}
